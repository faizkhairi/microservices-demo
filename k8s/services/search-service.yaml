apiVersion: v1
kind: Service
metadata:
  name: search-service
  namespace: microservices-demo
spec:
  selector:
    app: search-service
  ports:
    - protocol: TCP
      port: 4005
      targetPort: 4005
  type: ClusterIP
