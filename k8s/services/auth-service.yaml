apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: microservices-demo
spec:
  selector:
    app: auth-service
  ports:
    - protocol: TCP
      port: 4001
      targetPort: 4001
  type: ClusterIP
